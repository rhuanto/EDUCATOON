{
  "name": "educatoon-angular-node-sqlite",
  "version": "1.0.0",
  "private": true,
  "description": "Demo local Educatoon: Angular frontend + Node.js backend + SQLite",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run start --prefix frontend",
    "reset:db": "node backend/src/server.js --reset"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
